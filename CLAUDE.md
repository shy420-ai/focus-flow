# focus-flow-react

React migration of focus-flow. Built with Vite + React + TypeScript + Zustand + Tailwind CSS v4.

## Dev

```bash
pnpm install
cp .env.example .env.local  # fill in Firebase values
pnpm dev
```

## Deploy

Pushes to `main` auto-deploy to GitHub Pages via GitHub Actions.
